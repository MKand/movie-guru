apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: booksguru
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
      - name: server
        image: "{{ .Values.Config.Image.Repository }}/chatserver:{{ .Values.Config.Image.Tag }}"
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: POSTGRES_HOST
          value: "db-service.booksguru.svc.cluster.local"
        - name: PROJECT_ID
          value: {{ .Values.Config.projectID }}
        - name: POSTGRES_DB_USER_PASSWORD
          value: "minimal"
        - name: POSTGRES_DB_USER
          value: "minimal-user"
        - name: POSTGRES_DB_NAME
          value: "fake-movies-db"
        - name: TABLE_NAME
          value: "movies"
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_HOST
          value: "cache-service.booksguru.svc.cluster.local"
        - name: FLOWS_URL
          value: "http://flows-service.booksguru.svc.cluster.local:3400"
        - name: LOCATION
          value: {{ .Values.Config.geminiApiLocation }}
        - name: ENABLE_METRICS
          value: "true"
        - name: OTEL_EXPORTER_OTLP_INSECURE
          value: "true"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://otel-opentelemetry-collector.otel.svc.cluster.local:4318
        - name: FEEDBACK_URL
          value: ""
---
apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: booksguru
spec:
  selector:
    app: server 
  ports:
  - protocol: TCP
    port: 80 
    targetPort: 8080
  type: ClusterIP
