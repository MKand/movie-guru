apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movie-guru-chart.fullname" . }}-test-connection"
  labels:
    {{- include "movie-guru-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movie-guru-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
